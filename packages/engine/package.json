{
  "name": "@big-plan/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.lib.json",
    "lint": "eslint src test",
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.lib.json --noEmit"
  },
  "nx": {
    "tags": [
      "type:app"
    ]
  },
  "dependencies": {
    "@big-plan/agent-contracts": "workspace:*",
    "@big-plan/code-context": "workspace:*",
    "@big-plan/git": "workspace:*",
    "@big-plan/github": "workspace:*",
    "@big-plan/repository-authority": "workspace:*",
    "@big-plan/repository-tooling": "workspace:*",
    "@big-plan/source-index": "workspace:*",
    "ajv": "8.20.0"
  }
}
